# R1: fine-tune SmolLM2 on the parse residual. MLX rather than torch/transformers: this is an
# M1 Max, and MLX-LM is the proven path from chem_sage and chatPDB with native --mask-prompt
# and --report-to wandb.
mlx>=0.18
mlx-lm>=0.19
wandb>=0.17
huggingface_hub>=0.24
